name: CI push Laravel App to registry.offene-werkstaetten.org

on:
  push:
    branches:
      - "main"

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository 🕹️
        uses: actions/checkout@v3

      - name: Nodejs Build Frontend 🚧
        run: |
          cd ./laravel
          npm ci --legacy-peer-deps
          npm run build --if-present

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to registry.makakken.de Hub
        uses: docker/login-action@v2
        with:
          registry: registry.makakken.de
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_TOKEN }}

      - name: Build 🚜
        uses: docker/build-push-action@v4
        with:
          context: ./laravel
          push: true
          tags: registry.makakken.de/dca-laravel:latest

      # - name: Deploy 🚀
      #   uses: appleboy/ssh-action@v0.1.7
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USER }}
      #     key: ${{ secrets.SSH_KEY }}
      #     port: ${{ secrets.PORT }}
      #     script: docker-compose pull && docker-compose down && docker volume rm dca-makakken-de_app_data && docker-compose up --force-recreate -d
